export enum RoutesEnum {
  Home = '/',
  Projects = '/projects',
  Screens = '/screens',
}

export enum buttonTypeEnum {
  button = 'button',
  submit = 'submit',
  reset = 'reset',
}

export enum x {}
export enum y {}
